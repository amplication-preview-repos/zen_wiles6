/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { CheckIn } from "./CheckIn";
import { CheckInCountArgs } from "./CheckInCountArgs";
import { CheckInFindManyArgs } from "./CheckInFindManyArgs";
import { CheckInFindUniqueArgs } from "./CheckInFindUniqueArgs";
import { CreateCheckInArgs } from "./CreateCheckInArgs";
import { UpdateCheckInArgs } from "./UpdateCheckInArgs";
import { DeleteCheckInArgs } from "./DeleteCheckInArgs";
import { Event } from "../../event/base/Event";
import { Member } from "../../member/base/Member";
import { CheckInService } from "../checkIn.service";
@graphql.Resolver(() => CheckIn)
export class CheckInResolverBase {
  constructor(protected readonly service: CheckInService) {}

  async _checkInsMeta(
    @graphql.Args() args: CheckInCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [CheckIn])
  async checkIns(
    @graphql.Args() args: CheckInFindManyArgs
  ): Promise<CheckIn[]> {
    return this.service.checkIns(args);
  }

  @graphql.Query(() => CheckIn, { nullable: true })
  async checkIn(
    @graphql.Args() args: CheckInFindUniqueArgs
  ): Promise<CheckIn | null> {
    const result = await this.service.checkIn(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => CheckIn)
  async createCheckIn(
    @graphql.Args() args: CreateCheckInArgs
  ): Promise<CheckIn> {
    return await this.service.createCheckIn({
      ...args,
      data: {
        ...args.data,

        event: args.data.event
          ? {
              connect: args.data.event,
            }
          : undefined,

        member: args.data.member
          ? {
              connect: args.data.member,
            }
          : undefined,
      },
    });
  }

  @graphql.Mutation(() => CheckIn)
  async updateCheckIn(
    @graphql.Args() args: UpdateCheckInArgs
  ): Promise<CheckIn | null> {
    try {
      return await this.service.updateCheckIn({
        ...args,
        data: {
          ...args.data,

          event: args.data.event
            ? {
                connect: args.data.event,
              }
            : undefined,

          member: args.data.member
            ? {
                connect: args.data.member,
              }
            : undefined,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.Mutation(() => CheckIn)
  async deleteCheckIn(
    @graphql.Args() args: DeleteCheckInArgs
  ): Promise<CheckIn | null> {
    try {
      return await this.service.deleteCheckIn(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }

  @graphql.ResolveField(() => Event, {
    nullable: true,
    name: "event",
  })
  async getEvent(@graphql.Parent() parent: CheckIn): Promise<Event | null> {
    const result = await this.service.getEvent(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }

  @graphql.ResolveField(() => Member, {
    nullable: true,
    name: "member",
  })
  async getMember(@graphql.Parent() parent: CheckIn): Promise<Member | null> {
    const result = await this.service.getMember(parent.id);

    if (!result) {
      return null;
    }
    return result;
  }
}
